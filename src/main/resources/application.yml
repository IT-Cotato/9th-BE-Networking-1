spring:
  application:
    name: networking1

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/real_estate?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: 3412
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace

